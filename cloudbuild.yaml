steps:
  # Docker 이미지 빌드
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/mega-schedule-api:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/mega-schedule-api:latest'
      - '.'
  
  # Docker 이미지 푸시 (Container Registry)
  - name: 'gcr.io/cloud-builders/docker'
    args: 
      - 'push'
      - 'gcr.io/$PROJECT_ID/mega-schedule-api:$COMMIT_SHA'
  
  # Cloud Run에 배포
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'mega-schedule-api'
      - '--image'
      - 'gcr.io/$PROJECT_ID/mega-schedule-api:$COMMIT_SHA'
      - '--region'
      - 'asia-northeast3'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8000'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '3600'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--concurrency'
      - '80'
      - '--update-env-vars'
      - 'DATABASE_URL=${_DATABASE_URL}'
      - '--update-env-vars'
      - 'GOOGLE_CLIENT_ID=${_GOOGLE_CLIENT_ID}'
      - '--update-env-vars'
      - 'FRONTEND_URLS=${_FRONTEND_URLS}'

images:
  - 'gcr.io/$PROJECT_ID/mega-schedule-api:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/mega-schedule-api:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  
# 대체 변수 (트리거에서 설정해야 함)
substitutions:
  _DATABASE_URL: 'postgresql://postgres:megastudy1234@db.dqunymmpnwuraghnzlec.supabase.co:5432/postgres'
  _GOOGLE_CLIENT_ID: '886486238835-cen5lkt74cveejd78egc9n57a99tp33p.apps.googleusercontent.com'
  _FRONTEND_URLS: 'https://pygame-7e3ab.web.app,https://pygame-7e3ab.firebaseapp.com'

